# OpenClaw Monitor BFF/API base URL
OPENCLAW_API_BASE=http://localhost:4110

# Gateway WebSocket endpoint used by realtime sync
NEXT_PUBLIC_OPENCLAW_WS_URL=ws://localhost:4110/gateway/events

# When true, /api/sessions masks sensitive fields like `lastTo`
MASK_SENSITIVE_FIELDS=false
